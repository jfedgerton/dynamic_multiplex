`stability_calibration_table.csv` is the calibration lookup behind
`partition_stability()`: columns level (partition_nmi, partition_ari,
node_jaccard), stab_lo, stab_hi, n_calib, acc_median, acc_q05, source. Written
by `replication/post/12_stability.R` and copied here by
`replication/run_all.sh post`. Provisional until the front-to-end rerun.
