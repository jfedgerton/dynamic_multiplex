% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_multilayer_visualizations.R
\name{plot_multilayer_alluvial}
\alias{plot_multilayer_alluvial}
\title{Plot an alluvial view of community transitions over time}
\usage{
plot_multilayer_alluvial(fit, max_nodes = NULL, palette = "Dark2")
}
\arguments{
\item{fit}{Fit object from \code{fit_multilayer_jaccard()},
\code{fit_multilayer_overlap()}, or \code{fit_multilayer_identity_ties()}.}

\item{max_nodes}{Optional cap on number of nodes to include for readability.}

\item{palette}{ColorBrewer qualitative palette, one of \code{"Set2"} or \code{"Dark2"}.}
}
\value{
A \code{ggplot} object.
}
\description{
Plot an alluvial view of community transitions over time
}
\examples{
if (requireNamespace("ggplot2", quietly = TRUE) &&
    requireNamespace("ggalluvial", quietly = TRUE)) {
  set.seed(123)
  layers <- lapply(1:3, function(i) {
    m <- matrix(rbinom(64, 1, 0.35), nrow = 8)
    m <- pmax(m, t(m))
    diag(m) <- 0
    m
  })
  fit <- fit_multilayer_jaccard(layers, algorithm = "louvain")
  p <- plot_multilayer_alluvial(fit)
}

}
