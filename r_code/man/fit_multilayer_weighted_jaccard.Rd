% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_multilayer_weighted_jaccard.R
\name{fit_multilayer_weighted_jaccard}
\alias{fit_multilayer_weighted_jaccard}
\title{Fit multilayer communities and interlayer node-strength weighted
Jaccard ties}
\usage{
fit_multilayer_weighted_jaccard(
  layers,
  algorithm = c("louvain", "leiden"),
  layer_links = NULL,
  min_similarity = 0,
  resolution_parameter = 1,
  directed = FALSE,
  add_self_loops = TRUE,
  self_loop_multiplier = 1,
  objective = NULL
)
}
\arguments{
\item{layers}{List of \code{igraph} objects or square adjacency matrices.}

\item{algorithm}{Community algorithm: \code{"louvain"} or \code{"leiden"}.}

\item{layer_links}{Optional data.frame defining which layers to connect,
with columns \code{from}, \code{to}, and optional \code{weight}. If \code{NULL}, adjacent layers
are connected in sequence.}

\item{min_similarity}{Minimum weighted similarity required to keep an
interlayer tie.}

\item{resolution_parameter}{Leiden resolution parameter.}

\item{directed}{Logical; if \code{TRUE}, build directed graphs from adjacency
matrices.}

\item{add_self_loops}{Logical; if \code{TRUE}, add intra-layer community
self-loop ties.}

\item{self_loop_multiplier}{Numeric multiplier applied to self-loop weighted
ties.}

\item{objective}{One of "cpm" or "modularity" for directed networks only}
}
\value{
A list with detected communities per layer and interlayer ties.
}
\description{
Runs Louvain or Leiden community detection for each layer and
creates interlayer ties between communities in selected layer pairs using
node-strength weighted Jaccard similarity.
}
\examples{
set.seed(123)
layers <- lapply(1:3, function(i) {
  w <- matrix(runif(64), 8, 8) * matrix(rbinom(64, 1, 0.35), 8, 8)
  w <- (w + t(w)) / 2
  diag(w) <- 0
  w
})
fit <- fit_multilayer_weighted_jaccard(layers, algorithm = "louvain")
names(fit)

}
