% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_multilayer_visualizations.R
\name{animate_multilayer_gif}
\alias{animate_multilayer_gif}
\title{Animate multiplex networks as a GIF by layer}
\usage{
animate_multilayer_gif(
  layers,
  fit = NULL,
  community_memberships = NULL,
  output_file,
  directed = FALSE,
  fps = 2,
  width = 800,
  height = 600,
  layout = "layout_with_fr",
  palette = "Dark2"
)
}
\arguments{
\item{layers}{List of \code{igraph} objects or square adjacency matrices.}

\item{fit}{Optional fit object from \code{fit_multilayer_jaccard()},
\code{fit_multilayer_overlap()}, or \code{fit_multilayer_identity_ties()}.}

\item{community_memberships}{Optional list of membership vectors
(one per layer). Ignored when \code{fit} is provided.}

\item{output_file}{Output GIF file path. Required; the function writes the
animation to this location and nowhere else. Use
\code{tempfile(fileext = ".gif")} to write to the session temporary directory.}

\item{directed}{Logical; if \code{TRUE}, adjacency matrices are treated as
directed.}

\item{fps}{Frames per second.}

\item{width}{Width of GIF in pixels.}

\item{height}{Height of GIF in pixels.}

\item{layout}{\code{igraph} layout function name. Defaults to \code{"layout_with_fr"}.}

\item{palette}{ColorBrewer qualitative palette, one of \code{"Set2"} or \code{"Dark2"}.}
}
\value{
The path to the created GIF.
}
\description{
Creates one frame per layer and colors nodes by community using
colorblind-friendly ColorBrewer palettes (\code{Set2} or \code{Dark2}).
}
\examples{
\donttest{
if (requireNamespace("ggplot2", quietly = TRUE) &&
    requireNamespace("gganimate", quietly = TRUE) &&
    requireNamespace("gifski", quietly = TRUE)) {
  set.seed(123)
  layers <- lapply(1:3, function(i) {
    m <- matrix(rbinom(64, 1, 0.35), nrow = 8)
    m <- pmax(m, t(m))
    diag(m) <- 0
    m
  })
  fit <- fit_multilayer_jaccard(layers, algorithm = "louvain")
  gif_path <- animate_multilayer_gif(
    layers,
    fit = fit,
    output_file = tempfile(fileext = ".gif"),
    fps = 2,
    width = 200,
    height = 150
  )
}
}

}
